services:
  app:
    build: .
    container_name: my-project
    restart: unless-stopped
    network_mode: host
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3099/api/state"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
